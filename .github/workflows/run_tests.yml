name: Run Tests

on:
  # allows running workflows manually
  workflow_dispatch:

  # local-develop is where this fork's work lands before going upstream, so it
  # needs checking too; without it a PR targeting it reports no checks at all.
  pull_request:
    branches:
      - main
      - local-develop
  push:
    branches:
      - main
      - local-develop

jobs:
  main:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13", "3.14", "3.14t"]

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Set up virtual environment
        run: |
          python -m pip install -U pip
          python -m venv venv
          source venv/bin/activate

      - name: Install
        run: |
          python -m pip install .[test]

      - name: Test
        run: python -m pytest -ra tests
